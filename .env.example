# ---------------------------------------------------------
# Multi-Agent Orchestrator — Environment Variables
# ---------------------------------------------------------
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# ---------------------------------------------------------

# PostgreSQL connection string (must match docker-compose.yml credentials)
DATABASE_URL=postgresql://orchestrator:orchestrator@localhost:5433/orchestrator

# Fernet encryption key for api_credentials.api_key column.
# Generate one with:  python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Shared secret for authenticating MCP clients.
# Any strong random string, e.g.:  python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_AUTH_TOKEN=

# ---------------------------------------------------------
# Test database (used by pytest — same Postgres instance,
# separate database created by docker/init-test-db.sql)
# ---------------------------------------------------------
TEST_DATABASE_URL=postgresql://orchestrator:orchestrator@localhost:5433/orchestrator_test

# ---------------------------------------------------------
# Read-only Web Dashboard Authentication (HTTP Basic Auth)
# ---------------------------------------------------------
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=orchestrator_secret

# ---------------------------------------------------------
# Hybrid AI Project Manager (PM LLM)
# ---------------------------------------------------------
# LOCAL DEV CONVENIENCE ONLY:
# If you run Ollama locally on your developer machine, set:
# PRIMARY_PM_URL=http://localhost:11434/v1
# PRIMARY_PM_MODEL=openai/llama3.1
# Note: On hosted production (e.g. Render), PRIMARY_PM_URL is omitted or unset.

# CLOUD FALLBACK CHAIN (Used in Production & whenever local Ollama is absent):
# Priority 1: Groq (Free tier API key from https://console.groq.com)
FALLBACK_PM_KEY=
FALLBACK_PM_MODEL=groq/openai/gpt-oss-20b

# Priority 2: Google Gemini (Free tier API key from Google AI Studio)
GEMINI_API_KEY=
GEMINI_PM_MODEL=gemini/gemini-3.8-flash

# Priority 3: OpenRouter (Guaranteed free-tier model)
OPENROUTER_API_KEY=
OPENROUTER_PM_MODEL=openrouter/meta-llama/llama-3.3-70b-instruct:free

